import { Signika, League_Spartan } from 'next/font/google'

export const signika = Signika({ subsets: ['latin'] })

export const leagueSpartan = League_Spartan({
  subsets: ['latin'],
  weight: ['900'],
})
